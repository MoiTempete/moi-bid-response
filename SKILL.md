# 投标技术响应文件编写

## 这个 Skill 做什么

根据用户提供的招标技术规格书（docx），自动完成：

1. **解析招标文件与硬约束抽取** — 提取章节结构、条款编号、重点标记、附件格式/响应模板，并抽取工期/日期/有效期等硬约束、检测条款冲突
2. **生成响应大纲** — 优先按招标自带模板组织，标注核心/制式分类 + 篇幅配额 + 招标条款覆盖映射
3. **逐章编写响应** — 按大纲顺序，采用综合叙述风格，核心优先，每章写完后用户可即时调整
4. **编制技术响应偏离表** — 逐条对标，正/负偏离判定，范围边界澄清
5. **输出 Word 文档** — 直读 markdown 生成，无中间格式，生成后自动双向校验
6. **交付前自检** — 自创交付物检测、引用依据审计、内容完整性核对

## 输入要求

用户需要提供：

- **招标文件 docx 路径**（必需）：技术规格书或招标文件的技术部分
- **招标文件正文**（强烈建议）：招标公告、投标人须知中常含工期/资格/偏离表等关键约束
- **参考素材**（可选但强烈建议）：往期同类标书、产品资料、子系统资料
- **项目/公司信息**（可选）：项目名称、投标公司名称、拟定品牌型号
- **排版要求**（可选）：字体、字号、缩进、行距等，见 `doc_config.json`
- **特殊格式要求**（可选）：如招标规定了响应表格格式

## 关键概念

### 三种响应组织方式

**方式 A — 按招标自带模板组织**：
当招标文件包含"附件格式"、"响应文件格式"、"服务方案应包括"等明确的响应模板时，直接按该模板组织。

**方式 B — 软件工程式组织**（招标无模板时优先采用）：
```
1. 项目概述
2. 总体设计原则
3. [可选] 产品生产工艺流程方案
4. 产品功能方案
5. 技术性能方案
6. 设计制造标准与安全方案
7. 项目实施与验收方案
8. 售后服务方案
```

**方式 C — 按招标章节逐章对应**：
当用户明确要求按招标书结构组织，或招标书结构非常清晰时使用。

> ⚠ **模板与深度的取舍（实战高频问题）**：招标自带模板常是"服务方案"型（如9项：项目理解、服务范围、组织架构、人员、质量保障、重难点…），**其下没有技术方案的独立位置**，而技术评分点却集中在技术规格书里。此时**一级目录不得增删**（否则不响应模板），正确做法是在模板项之下补充技术二级/三级章节，例如把"总体技术方案、五子系统接入、数据采集、组态开发、平台集成、网络安全、供配电"全部放在"项目服务范围、具体工作内容界定"之下。**不要为了放技术方案而新增一级目录。**

### 两种合规标注风格

**风格 1 — 隐式叙述**：不标注"完全响应"，直接以技术叙述方式描述实现方案。

**风格 2 — 显式引用**（推荐，且经实战验证有差异化价值）：
每节开头用一句"本章节满足'...'的相关要求"明确对标，然后展开技术叙述。

> **实战结论**：在对同一项目的多份标书做比对时，"本章节满足…"是识别度最高的差异化特征——同类标书往往全部不用。**用户无偏好时默认风格 2**，它同时便于评委逐条核对。

### 建设边界的下限与上限（避免过度设计的关键）

编写子系统/功能类内容时，必须同时守住两条边界：

| | 边界 | 含义 |
|---|---|---|
| **下限** | 技术规格书 | 规格书要求的接入范围、测点、控制功能、画面、指标，**逐项落实，不减项降标** |
| **上限** | 既有能力（子系统资料／产品资料） | **不新增设备侧不具备的测点与控制功能，不改变设备侧原有控制与联锁逻辑** |

**集控/集成类项目的基本定位**：把设备侧已建成的数据与能力**对接过来**，集中展示、集中控制、并与上层平台集成；基础能力依赖设备侧 PLC／上位机／工控机。据此：

- 子系统侧**不轻易扩展**；确需扩展时，只选**价值高、成本低、原厂商能对接**的一两个点；
- 凡扩展升级内容，必须**要么有规格书依据，要么成本极低**；
- **不引入规格书未要求且素材中属他类项目专用的能力**（如把井工矿的矿压监测、综采综掘、三维数字孪生塞进地面工业广场项目）。

### 核心编写原则

- 将一个章节内的多条相关要求**融合为连贯的技术叙述**
- **比招标书更具体**：增加具体数字、指标、流程细节
- **核心功能独立展开**：每个关键场景/功能单独成段
- **可选增值章节**：根据项目特点可增加"设计原则"、"生产工艺流程"、"创新性说明"、"应急保障"等

---

## 工作流

### Step 1 · 解析招标文件与硬约束抽取

#### 1a. 结构化解析

```bash
python3 scripts/parse_tender.py <招标文件.docx>            # 友好输出
python3 scripts/parse_tender.py <招标文件.docx> --json > parsed.json
```

输出包含：章节层级树、重点标记条目、**强制条款**（应/须/必须/不得…）、标题索引。

> **注意重点标记的可靠性**：若规格书用**标题加粗**而非★号标注，直接统计加粗会把目录树和封面拆字误报为"重点条款"。本脚本已做三重过滤：封面噪声（单字/编制/审核/日期）、纯标题行、以及区分"标题加粗（版式，低权重）"与"正文加粗（实质强调）"。**当`emphasized_count`异常偏大时，应以`mandatory_items`（强制条款）作为编制偏离表与核查覆盖的主要依据。**

同时搜索是否含招标自带响应模板：`附件格式`、`响应文件格式`、`服务方案应包括`、`技术方案要求`、`偏离表`。通常在招标文件末尾（附表/附件部分）。

#### 1b. 硬约束抽取与冲突对账（**必做，不可跳过**）

```bash
python3 scripts/extract_constraints.py 招标文件.docx 技术规格书.docx
```

抽取全部日期/工期/有效期/质保期/响应时效条款，按主题归组并检测冲突。

> **为什么必做（真实事故）**：某招标文件公告写"11月15日前完成整体施工"，投标人须知又写"自合同签订之日起60个日历日内完成"，而开标日为9月24日——按60个日历日排计划会**直接突破11月15日**。编写者只抓住其中一条，排出的进度计划就是错的，属实质性不响应。
>
> **处置原则**：存在**绝对日期节点**时，该节点优先级最高，全部计划以它**倒排**；相对期限与绝对节点取更早者。计划表应同时给出"计划日期"与"相对天数"两列，与合同实际签订日解耦。

#### 1c. 素材能力清单提取（有参考素材时）

素材总量超过约 10 万字时，**优先用 subagent 并行提取**，避免上下文溢出：

- 一份素材一个 subagent，各自产出一份结构化能力清单；
- 制式素材（往期标书）→ 提取资质清单、组织与人员、售后体系、质量目标、实施方法论、培训保密等，**保留原文措辞与具体数值**；
- 技术素材（产品/子系统资料）→ 提取技术路径、协议、参数、**范围边界条款**，并标注"仅适用于某类项目 vs 可通用迁移"。

也可先用脚本出骨架：

```bash
python3 scripts/extract_capability.py 参考素材A.docx 参考素材B.docx --dir 子系统资料 -o 素材能力清单.md
```

> ⚠ **素材数值必须逐项替换**：往期标书中的质保年限、响应时效、可用率、并发规模、团队人数等**几乎都与本项目招标要求不同**。直接搬入会造成隐蔽的实质性偏离。用 `check_invented.py --leak` 扫描残留（见 Step 5b）。

#### 1d. 条款冲突识别

除 Step 1b 的日期冲突外，还需识别**语义层面的条款冲突**，典型两类：

1. **规格书内部冲突**：同一对象在不同章节被赋予不同要求。例：某章要求"变电站仅监不控"，另一章又要求"箱式变电站可远方控制"。→ 辨析二者指向的**层面**（操作边界 vs 设备能力），分层面响应，并在偏离表"填写说明"中写清。
2. **规格书与既有资料冲突**：资料显示的能力与规格书描述不一致。例：规格书说"四台上位机"但正文只详述两类，资料显示实为四类（含五防系统、网络安全系统）；或资料要求"遥控遥调"而规格书明确"不开放遥控"。→ **以规格书为准**收敛，差异写入偏离表留痕；规格书未详述但资料界定的范围**按资料补全**（守住下限）。

**输出一张冲突清单**：冲突点 / 双方表述 / 以谁为准 / 处置方式 / 是否进偏离表。

**展示 1a~1d 结果给用户确认。**

---

### Step 2 · 风格配置（生成大纲前必须先确认）

**必须在生成大纲之前完成，不可跳过。**

#### 2a. 分析招标特征

| 特征维度 | 如何判断 | 影响什么 |
|---------|---------|---------|
| 有无附件格式 | 搜索"附件格式"、"响应文件格式"、"偏离表" | 决定组织方式 |
| 招标方类型 | "集团"→国企/集团；"煤矿"、"矿井"→技术型单位 | 合规标注风格 |
| 行业领域 | 关键词匹配 | 增值章节选择 |
| 条款密度 | 功能条款数量/总条款比 | 粒度建议 |
| 标星/加粗比例 | emphasized_count / heading_count | 核心/制式分类精度 |
| **硬约束冲突** | Step 1b 的冲突清单 | 进度计划与工期承诺 |
| **模板与深度是否匹配** | 模板项是否含技术方案位置 | 见 Step 2b 第三问 |

#### 2b. 呈现分析结果并询问用户（三问）

```markdown
## 🔍 招标文件分析

| 特征 | 检测结果 |
|------|---------|
| 附件格式/响应模板 | ✅ 检测到… / ❌ 未检测到 |
| 招标方类型 | 国企集团 / 技术型单位 / 政府 / 无法判断 |
| 行业领域 | … |
| 条款总数 | X 条，其中重点标记 Y 条、强制条款 Z 条 |
| 硬约束冲突 | ✅ 无 / ⚠ 发现 N 处（详见下表） |

## 🎨 请选择响应风格

### 一、组织方式
| 选项 | 说明 |
|------|------|
| **A. 按招标模板** | 以招标附件格式为骨架（推荐，检测到模板时） |
| **B. 软件工程式** | 设计原则→功能→性能→实施→售后 |
| **C. 章节对应式** | 按招标技术规格书章节一一对应 |

### 二、合规标注风格
| 选项 | 说明 |
|------|------|
| **1. 隐式叙述** | 技术口吻自然融入 |
| **2. 显式引用** | 每节"本章节满足'…'的相关要求"后展开（推荐，实战差异化最强） |

### 三、技术方案如何安放（检测到模板时必问）
| 选项 | 说明 |
|------|------|
| **A. 严守模板，技术方案置于模板项之下** | 一级目录不增删，技术内容作为二级/三级章节（推荐） |
| **B. 模板项 + 独立技术方案一级章节** | 技术更醒目，但一级目录与模板不完全一致 |
| **C. 完全按规格书章节组织** | 技术覆盖最全，但不响应招标模板 |
```

#### 2c. 记录配置

```
组织方式: [A/B/C]
合规风格: [1/2]
技术方案安放: [A/B/C]
交付节奏: [逐章确认 / 按一级章节批量确认]   ← 可配置，默认逐章
```

---

### Step 3 · 生成响应大纲（必须等用户确认）

#### 3a. 生成骨架

- **方式 A**：以招标模板章节为一级骨架，**必要时根据技术规格书在模板项之下补充细化**（不新增一级目录）
- **方式 B**：标准软件工程结构
- **方式 C**：按招标技术章节一一对应

#### 3b. 内容分类

| 分类 | 判断依据 |
|------|---------|
| `[核心·标星]` | 有加粗/标星/标色/关键词标记，或属强制条款 |
| `[核心]` | 技术方案/架构/功能指标等实质性内容 |
| `[制式·重点]` | 售后/培训/质保等通用内容但有特殊要求 |
| `[制式]` | 无特殊标记的通用标准条款 |

#### 3c. 篇幅配额（按相对比例，不按绝对字数）

**先估总字数，再按比例分配**——绝对字数表在大型项目中必然失真。

**总字数估算方法**：
```
总字数 ≈ 核心章节数 × 1800  +  制式章节数 × 800  +  技术子系统数 × 2500
再根据用户要求放大（如需"技术方案3倍"则核心部分再乘系数）
```

| 分类 | 占比 | 展开策略 |
|------|------|---------|
| `[核心·标星]` | 各章约占全文 8–15% | 每个子功能独立段落，含具体指标 |
| `[核心]` | 各章约占 4–8% | 综合叙述 + 关键技术参数 |
| `[制式·重点]` | 各章约占 2–4% | 标准化展开 + 针对性细化 |
| `[制式]` | 各章约占 1–2% | 标准模板，简洁清晰 |

**写作过程中累计核对**：每完成 2–3 章即累计实际字数与配额对比，偏差超 30% 时及时向用户说明并调整后续配额，**不要等到全部写完才发现总量翻倍**。

#### 3d. 覆盖映射

在大纲中增加一列，标注每个响应章节覆盖了哪些招标条款，确保无遗漏。**强制条款必须 100% 有归属。**

#### 3e. 输出格式

沿用既有大纲模板，另在"汇总"表中增加两行：
- **强制条款覆盖率**（应 100%）
- **预估总字数**及其在核心/制式间的分配

#### 3f. 等待用户确认

**必须等待用户明确确认**才能进入 Step 4。用户只回复"确认"/"OK"/"开始"时直接进入 Step 3.5。

---

### Step 3.5 · 技术响应偏离表（独立交付物，不可省略）

偏离表几乎是每份招标的必备交付物，且招标文件通常规定"**未在偏离表中列明的偏离项，视为完全响应**"——漏报即承担兜底责任。

```bash
python3 scripts/generate_deviation_table.py 技术规格书.docx -o 偏离表骨架.md
```

**编制要求**：

1. **逐条填写**"投标文件的技术条款"列，写明响应位置与内容提要；
2. **响应状态三类**：`完全响应` / `正偏离（增值扩展）` / `负偏离（须说明理由）`；
3. **凡招标未要求而自行增设的内容，一律标为正偏离**，避免被误认为负偏离；
4. **范围边界澄清**（如某子系统仅监不控、某项费用不含在本项目内）须在表后"**填写说明**"中逐条说明，写明"该内容不构成对招标要求的偏离"及理由；
5. 表头列名若招标有规定，用 `--headers` 指定。

> **实战提醒**：偏离表是应对"招标文件内部条款冲突"的正式留痕位置。凡 Step 1d 识别出的冲突，其处置结论都应在此留下书面说明。

---

### Step 4 · 逐章编写响应

**编写顺序**：先写所有 `[核心]` 类别章节，再写 `[制式]` 类别章节。
**交付节奏**：按 Step 2c 的配置执行（逐章确认 / 按一级章节批量确认）。

#### 编写原则

**通用原则**：

1. **比招标书更具体**：能加数字的地方加数字，能列举的地方列举
2. **核心功能独立展开**：每个关键功能/场景各自成段，有独立标题和指标
3. **制式内容标准化但完整**：仍要具体（故障分级、响应时效、人员安排）
4. **不确定处留标记**：无法确认的标 `【待补充】`；需用户提供的标 `【待填写】`

**⚠ 边界原则（防止过度设计，实战最高频问题）**：

5. **严守下限与上限**（见"关键概念·建设边界的下限与上限"）：不新增设备侧不具备的能力，不改变设备侧控制逻辑；
6. **不发明交付物**：**严禁**编造规格书未要求的确认表、验证报告、方案书等带书名号的交付物。需要表达"形成记录"时用通用表述（"形成核查记录"），**不要写《XX核查记录》**——书名号会让它变成合同义务。生成后必须用 `check_invented.py` 复核；
7. **不自行加码指标**：性能指标以规格书列明的为准，不自创"十项指标""十二项成效指标"之类的考核体系；
8. **扩展必须有依据或极低成本**：确需增值扩展时，只选一两个"价值高、成本低、原厂商能对接"的点，并在偏离表中标为正偏离。

**风格 1（隐式叙述）特有原则**：

9. **综合叙述，不逐条标注**，用"我方""本项目""系统"为主语做正面描述

**风格 2（显式引用）特有原则**：

10. **逐节对标，先引用后展开**：每节开头"本章节满足'（X）XXX'中'（X）XXX：...'的相关要求。"，引文须引用招标原文完整内容；然后展开技术叙述，避免写成逐条"要求→响应"格式

#### 每章编写流程

1. **回顾招标原文**：找到该章覆盖的所有招标原条款，仔细阅读
2. **查阅编写规范**：参考 `references/writing-guide.md`
3. **查阅素材能力清单**：需要引用公司资质、人员、售后体系时，从 Step 1c 的清单取值，**数值逐项核对本项目要求**
4. **生成响应内容**：按选定风格撰写，控制篇幅在配额范围内（±20%）
5. **自检覆盖度**：确认覆盖映射中标注的招标条款均已纳入，**强制条款无遗漏**
6. **展示草稿**：以清晰 Markdown 格式输出，等待用户反馈

---

### Step 5 · 汇总输出 Word 文档

#### ⚠ 关键变更：使用直读生成，禁用 JSON 中间格式

`generate_docx.py` 的 JSON 输入路径在内容超过约 1.5 万字时存在**静默丢内容**风险（文档能打开但尾部或部分章节内容消失）。**一律使用直读脚本**：

```bash
# 1) 生成配置模板（首次）
python3 scripts/generate_docx_direct.py --init doc_config.json

# 2) 编辑 doc_config.json：配置排版 + 声明每个输出文档及其章节
# 3) 生成
python3 scripts/generate_docx_direct.py --config doc_config.json
```

**零配置快速模式**：把 `NN-*.md` 直接合成一个文档：

```bash
python3 scripts/generate_docx_direct.py --auto --title "XX项目技术响应文件"
```

**分章生成后合并**：

```bash
python3 scripts/generate_docx_direct.py --config part1.json
python3 scripts/generate_docx_direct.py --config part2.json
python3 scripts/merge_docx.py -o 完整文档.docx part1.docx part2.docx --page-break
```

#### 排版配置（`doc_config.json` 的 `format` 段）

默认值即为下列常用设置，可整体或逐项覆盖：

| 元素 | 默认 |
|------|------|
| 正文 | 仿宋 小四(12pt)，首行缩进 **2 字符**（`firstLineChars`，非固定值） |
| 标题 | 黑体 四号(14pt)，**不缩进** |
| 封面文档标题 | 黑体 四号(14pt)，居中（**若与一级目录同号不够醒目，可单独调大 `title_size_pt`**） |
| 全文英文 | Times New Roman |
| 行距 / 段距 | 1.5 倍 / 段前段后 0 |
| 编号 | 国标多级自动编号（H2 一、 H3 1. H4 1.1. H5 1.1.1.） |
| 表格 | 10.5pt，表头加粗居中 |
| 页面 | A4，上下 2.5cm，左右 3.2cm |

> 常用备选排版：正文宋体小四、标题黑体三号、首行缩进用固定值 0.74cm（把 `first_line_chars` 设为 0 并调整缩进项）。

#### 章末工作标记

`generate_docx_direct.py` 会自动丢弃 `（第X章完）` 之类的编辑痕迹（见 `drop_line_patterns`），不需手工清理空行。

---

### Step 5b · 交付前自检（三查，**必做**）

长文档交付前必须完成三项检查，任一不过不得交付。

#### 第一查 · 内容完整性（源 ↔ 产物双向核对）

```bash
python3 scripts/verify_docx.py --config doc_config.json
```

三重核对：① 段落级存在性；② 表格数/单元格数/单元格中文字数（XML 直数，合并单元格只算一次）；③ 总字数差异**完整归因**。

> **判读标准**：报告中"**★ 无法解释的差异**"必须为 **0**。非零即存在静默丢失，必须排查到归因为止。常见可解释项：剥离"第X章"前缀（转自动编号）、剥离章末工作标记、新增封面标题。
>
> **注意两个统计陷阱**：① markdown 表格行会被"按句号切句"的统计误算成超长句，属口径假象；② python-docx 遍历 `row.cells` 会对合并单元格重复计数，统计表格必须直接遍历 XML 的 `w:tc`。

#### 第二查 · 自创交付物与素材数值残留

```bash
python3 scripts/check_invented.py --draft "0*.md" \
    --sources 技术规格书.docx 招标文件.docx 参考素材A.docx 子系统资料/ \
    --leak 99.99 "30000在线" "1年质保" RTO RPO
```

> **判读标准**：凡标记"★ 疑似自创"的《》名称，逐一确认。招标未要求且非行业惯例交付物的，**去除书名号改为通用表述**。此检查可筛出数十个隐蔽的合同义务。

#### 第三查 · 引用依据审计（应对外部质疑）

```bash
python3 scripts/audit_citation.py --draft "0*.md" \
    --spec 技术规格书.docx --tender 招标文件.docx \
    --ref 参考素材A.docx 参考素材B.docx --sub 子系统资料/
```

输出分章有据率与来源分布。**自主撰写比例显著偏高的章节，应重点复查是否存在招标未要求的自创内容**（与第二查交叉验证）。

输出该报告的价值：当评审或内部合规审查质疑"是否套用他项目标书"时，可凭量化口径说明。

---

### Step 6 · 后续流程 — 生成述标 PPT（可选）

Step 5b 全部通过、Word 文档输出后，**主动**向用户建议：

> 📄 技术响应文件已生成并通过自检。是否需要基于这份响应文件生成**讲标答辩（述标）PPT**？可以用 `moi-bid-defense` 自动提取响应中的差异化亮点和量化指标，生成带讲标备注和预判 Q&A 的幻灯片。

检查与安装：

```bash
ls ~/.claude/skills/moi-bid-defense/SKILL.md 2>/dev/null && echo "已安装" || echo "未安装"
# 未安装时（征得同意后）：
git clone https://github.com/MoiTempete/moi-bid-defense ~/.claude/skills/moi-bid-defense
```

启动：

```bash
/moi-bid-defense <响应文件路径>
```

---

## 关键规则

1. **硬约束必须先对账**：编制进度计划前必须完成 Step 1b，日期冲突未消除不得动笔
2. **风格必须先确认**：Step 2 三问确认后才能进入 Step 3
3. **大纲必须等用户确认**，确认后才能进入编写
4. **偏离表独立成步**：不得只在正文写"完全响应"而不出偏离表
5. **逐章或批量确认**：按用户配置的节奏执行，不要一次性写完
6. **核心优先**：先写核心章节，再写制式章节
7. **依风格执行**：风格1用隐式叙述，风格2用显式引用，不混用
8. **比招标更具体**：增加数字、指标、流程细节
9. **严守下限与上限**：规格书要求不减项；设备侧不具备的能力不新增；不改变设备侧控制逻辑
10. **不发明交付物**：不编造规格书未要求的《》交付物；不自行加码考核指标；扩展须有依据或成本极低
11. **素材数值逐项替换**：参考往期标书时，质保期/响应时效/可用率/并发数等必须按本项目要求改
12. **不要编造**：不确定处标注 `【待补充】`，需用户提供的标 `【待填写】`
13. **确保全覆盖**：每条招标要求都应在响应中找到对应，**强制条款 100% 覆盖**
14. **生成必须用直读脚本**：禁止使用 JSON 中间格式处理长文档
15. **三查不过不交付**：内容完整性、自创交付物、引用审计，任一不通过不得交付
16. **Step 4 内容不可逆**：逐章确认后的内容即视为终稿，生成时严禁精简、压缩、合并段落或删改
17. **禁止空行**：段落之间不保留空白行；导出前可 `sed -i '' '/^$/d' <文件>` 批量去除
18. **完成后主动建议述标 PPT**

## 脚本速查

| 脚本 | 用途 | 关键参数 |
|------|------|---------|
| `parse_tender.py` | 解析招标文件结构、重点标记、**强制条款** | `<docx>` `--json` |
| `extract_constraints.py` | **硬约束抽取与日期冲突对账** | `<文件…>` `--json` |
| `extract_capability.py` | 参考素材能力清单提取 | `<文件…>` `--dir` `-o` |
| `generate_deviation_table.py` | **技术响应偏离表骨架** | `<规格书>` `-o` `--mandatory-only` `--headers` |
| `generate_docx_direct.py` | **直读生成 Word（默认路径）** | `--config` `--auto` `--init` |
| `generate_docx.py` | JSON 输入生成（**已降级为备选**） | 见文件头警示 |
| `merge_docx.py` | 合并多个 docx | `-o` `--glob` `--page-break` |
| `verify_docx.py` | **源↔产物双向核对** | `--config` 或 `--docx/--src` |
| `check_invented.py` | **自创交付物检测 + 素材数值残留扫描** | `--draft` `--sources` `--leak` |
| `audit_citation.py` | **引用依据占比审计** | `--draft` `--spec` `--tender` `--ref` `--sub` |

## 参考文件

- `references/writing-guide.md` — 响应编写规范、模板、篇幅控制指南
- `references/doc-generation-strategy.md` — 文档生成策略
